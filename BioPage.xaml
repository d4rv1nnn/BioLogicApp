<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiProject.BioPage"
             BackgroundColor="#121212"
             Title="Калибровка">

    <ScrollView>
        <StackLayout Spacing="25" Padding="20">
            <Label Text="Калибровка системы" 
                   TextColor="White" 
                   FontSize="28" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center"/>

            <!-- Section 1: Gender -->
            <Label Text="Пол" TextColor="Gray" FontSize="14" FontAttributes="Bold"/>
            <Grid ColumnDefinitions="*,*" ColumnSpacing="15">
                <!-- Male Card -->
                <Border x:Name="MaleCard" Stroke="#333333" BackgroundColor="#1E1E1E" StrokeShape="RoundRectangle 15" Padding="20">
                    <VerticalStackLayout HorizontalOptions="Center">
                        <Label Text="М" FontSize="30" TextColor="White" HorizontalOptions="Center"/>
                        <Label Text="Мужской" FontSize="12" TextColor="Gray" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnMaleSelected"/>
                    </Border.GestureRecognizers>
                </Border>
                
                <!-- Female Card -->
                <Border x:Name="FemaleCard" Stroke="#333333" BackgroundColor="#1E1E1E" StrokeShape="RoundRectangle 15" Padding="20" Grid.Column="1">
                    <VerticalStackLayout HorizontalOptions="Center">
                        <Label Text="Ж" FontSize="30" TextColor="White" HorizontalOptions="Center"/>
                        <Label Text="Женский" FontSize="12" TextColor="Gray" HorizontalOptions="Center"/>
                    </VerticalStackLayout>
                    <Border.GestureRecognizers>
                        <TapGestureRecognizer Tapped="OnFemaleSelected"/>
                    </Border.GestureRecognizers>
                </Border>
            </Grid>

            <!-- Section 2: Activity -->
            <Label Text="Активность" TextColor="Gray" FontSize="14" FontAttributes="Bold" Margin="0,10,0,0"/>
            <Border Stroke="#333333" BackgroundColor="#1E1E1E" StrokeShape="RoundRectangle 15" Padding="15">
                <VerticalStackLayout>
                    <Label x:Name="ActivityLabel" Text="Офис (Сидячий)" TextColor="#4CAF50" HorizontalOptions="Center" FontAttributes="Bold" FontSize="16"/>
                    <Slider x:Name="ActivitySlider" 
                            Minimum="0" Maximum="2" 
                            ValueChanged="OnActivityChanged"
                            MinimumTrackColor="#4CAF50" 
                            ThumbColor="#6200EE"/>
                </VerticalStackLayout>
            </Border>

            <!-- Section 3: Risk Factors -->
            <Label Text="Факторы риска" TextColor="Gray" FontSize="14" FontAttributes="Bold" Margin="0,10,0,0"/>
            <FlexLayout Wrap="Wrap" JustifyContent="Start" AlignItems="Start" Direction="Row">
                <!-- Chips (Buttons acting as chips) -->
                <!-- Alcohol -->
                <Button x:Name="RiskAlcohol" Text="Алкоголь" Clicked="OnRiskToggled" Style="{StaticResource RiskChipStyle}"/>
                <!-- Smoking -->
                <Button x:Name="RiskSmoking" Text="Курение" Clicked="OnRiskToggled" Style="{StaticResource RiskChipStyle}"/>
                <!-- Stress -->
                <Button x:Name="RiskStress" Text="Стресс" Clicked="OnRiskToggled" Style="{StaticResource RiskChipStyle}"/>
                <!-- Sleep -->
                <Button x:Name="RiskSleep" Text="Мало сна" Clicked="OnRiskToggled" Style="{StaticResource RiskChipStyle}"/>
            </FlexLayout>

            <!-- Finish Button -->
            <Button Text="Завершить настройку" 
                    BackgroundColor="#4CAF50" 
                    TextColor="White" 
                    CornerRadius="25" 
                    HeightRequest="55" 
                    FontAttributes="Bold"
                    Margin="0,20,0,0"
                    Clicked="OnFinishClicked"/>

        </StackLayout>
    </ScrollView>
    
    <ContentPage.Resources>
        <Style x:Key="RiskChipStyle" TargetType="Button">
            <Setter Property="BackgroundColor" Value="#1E1E1E"/>
            <Setter Property="TextColor" Value="Gray"/>
            <Setter Property="BorderColor" Value="#333333"/>
            <Setter Property="BorderWidth" Value="1"/>
            <Setter Property="CornerRadius" Value="20"/>
            <Setter Property="HeightRequest" Value="40"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,5,5"/>
            <Setter Property="Padding" Value="15,0"/>
        </Style>
    </ContentPage.Resources>
</ContentPage>
