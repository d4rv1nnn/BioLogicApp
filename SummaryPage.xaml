<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:BioLogicNative.Models"
             x:Class="BioLogicNative.SummaryPage"
             BackgroundColor="#121212"
             Title="Результат">

    <Grid RowDefinitions="Auto, *, Auto" Padding="20" RowSpacing="20">
        
        <!-- Header -->
        <Label Text="Диагноз сформирован" 
               FontSize="28" 
               FontAttributes="Bold" 
               TextColor="White"
               HorizontalOptions="Center" />

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Spacing="25">
                
                <!-- Strategy Block -->
                <Frame x:Name="StrategyFrame" BackgroundColor="#2D2D2D" CornerRadius="15" BorderColor="Transparent" Padding="15">
                    <VerticalStackLayout Spacing="10">
                        <Label Text="ВАЖНО: Порядок действий" 
                               TextColor="White" 
                               FontAttributes="Bold" 
                               FontSize="18"/>
                        
                        <Label x:Name="StrategyLabel"
                               Text="Анализ взаимосвязей..." 
                               TextColor="#FFA000" 
                               FontSize="16" />
                    </VerticalStackLayout>
                </Frame>

                <!-- List of Violations -->
                <Label Text="Обнаруженные дефициты:" TextColor="Gray" FontSize="14"/>
                
                <CollectionView x:Name="DetectedList">
                    <CollectionView.ItemTemplate>
                        <DataTemplate x:DataType="models:Hormone">
                            <Frame BackgroundColor="#1E1E1E" CornerRadius="10" Padding="15,10" Margin="0,0,0,10" BorderColor="Transparent">
                                <Label Text="{Binding Name}" TextColor="White" FontSize="16" FontAttributes="Bold"/>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Button -->
        <Button Grid.Row="2"
                Text="Открыть протокол лечения" 
                Clicked="OnOpenProtocolClicked"
                BackgroundColor="#00E676" 
                TextColor="Black" 
                FontAttributes="Bold" 
                CornerRadius="25" 
                HeightRequest="50"
                Margin="0,0,0,20"/>
    </Grid>

</ContentPage>
